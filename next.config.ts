import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Disable the dev indicator (bottom-right green square in development mode)
  devIndicators: false,

};

export default nextConfig;
